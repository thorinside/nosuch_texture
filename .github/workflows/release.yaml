name: Build and Release No Such Texture Plugin

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install ARM toolchain
        run: |
          brew update
          brew install --cask gcc-arm-embedded

      - name: Verify installation
        run: arm-none-eabi-g++ --version

      - name: Build plugin
        run: |
          cd NT
          make clean
          make hardware

      - name: Prepare release archive
        run: |
          mkdir -p release/programs/plug-ins/nosuch_texture
          cp NT/plugins/nosuch_texture.o release/programs/plug-ins/nosuch_texture/
          cd release
          zip -r ../nosuch_texture-plugin.zip programs
          cd ..
          echo "Archive contents:"
          unzip -l nosuch_texture-plugin.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: nosuch_texture-plugin.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
